# Code — {{.Name}}

Implementation for **{{.Name}}** lives in this directory (inline layout from `oz init`).

## Layout

Document how packages, services, or apps map to subdirectories so agents and contributors share the same mental model.

## Build & run

Add concrete commands for your stack (for example):

```bash
# examples — replace with your toolchain
# go build -o bin/app ./cmd/app
# npm install && npm run dev
```

## Tests

Describe how to run automated checks (`go test`, `pytest`, CI parity commands, etc.).
