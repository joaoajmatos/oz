min_accuracy: 1.0

queries:
  # --- coding agent ---
  - query: "implement the audit subcommand in Go"
    expected_agent: coding
    min_confidence: 0.7

  - query: "add a --json flag to the validate command"
    expected_agent: coding
    min_confidence: 0.7

  - query: "write unit tests for the workspace package"
    expected_agent: coding
    min_confidence: 0.7

  - query: "fix the bug in the scaffold template rendering"
    expected_agent: coding
    min_confidence: 0.7

  - query: "embed the new scoring.toml template into the binary"
    expected_agent: coding
    min_confidence: 0.7

  - query: "refactor the BM25F scorer into a separate file"
    expected_agent: coding
    min_confidence: 0.7

  - query: "add integration test for oz context build"
    expected_agent: coding
    min_confidence: 0.7

  # --- maintainer agent ---
  - query: "update AGENTS.md to reflect the new oz-context agent"
    expected_agent: maintainer
    min_confidence: 0.7

  - query: "check all agent definitions for missing required sections"
    expected_agent: maintainer
    min_confidence: 0.7

  - query: "OZ.md is out of date, update it to reflect current agents"
    expected_agent: maintainer
    min_confidence: 0.7

  - query: "audit the workspace structure against the oz convention"
    expected_agent: maintainer
    min_confidence: 0.6

  - query: "the skills directory is empty, flag it in open-items"
    expected_agent: maintainer
    min_confidence: 0.6

  - query: "review coding-guidelines.md for gaps as the project matures"
    expected_agent: maintainer
    min_confidence: 0.6

  # --- spec agent ---
  - query: "write an ADR for the decision to use BM25F for query scoring"
    expected_agent: spec
    min_confidence: 0.7

  - query: "update the project spec to reflect the new three-layer context architecture"
    expected_agent: spec
    min_confidence: 0.7

  - query: "crystallize the notes about the graph schema into canonical spec language"
    expected_agent: spec
    min_confidence: 0.7

  - query: "the oz context section of the spec is outdated, update it"
    expected_agent: spec
    min_confidence: 0.7

  - query: "add a new spec section describing the semantic overlay schema"
    expected_agent: spec
    min_confidence: 0.7

  - query: "promote the brainstorm notes about MCP into the architecture doc"
    expected_agent: spec
    min_confidence: 0.6

  - query: "bump the oz standard version in OZ.md after the breaking spec change"
    expected_agent: spec
    min_confidence: 0.5
    expected_candidates: ["spec", "maintainer"]
